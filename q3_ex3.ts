function calcularRaizQuadrada(valor: number): number {
    return Math.sqrt(valor);
  }

const valorDoFormulario: string = "4";
const resultado = calcularRaizQuadrada(valorDoFormulario); 
console.log(resultado);